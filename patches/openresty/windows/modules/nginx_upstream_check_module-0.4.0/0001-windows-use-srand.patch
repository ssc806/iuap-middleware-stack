diff --git a/ngx_http_upstream_check_module.c b/ngx_http_upstream_check_module.c
index a3c6177..897ab5b 100644
--- a/ngx_http_upstream_check_module.c
+++ b/ngx_http_upstream_check_module.c
@@ -4,6 +4,10 @@
 
 
 #include <nginx.h>
+#if (NGX_WIN32)
+#include <stdlib.h>
+#endif
+
 #include "ngx_http_upstream_check_module.h"
 
 
@@ -968,7 +972,11 @@ ngx_http_upstream_check_add_timers(ngx_cycle_t *cycle)
                    &peers->check_shm_name,
                    peers->peers.nelts);
 
+#if (NGX_WIN32)
+    srand((unsigned) ngx_pid);
+#else
     srandom(ngx_pid);
+#endif
 
     peer = peers->peers.elts;
     peer_shm = peers_shm->peers;
